# Vitest supplies safe defaults for tests that mock the database.
# Set this only for explicit database integration tests.
TEST_DATABASE_URL="postgresql://test:test@127.0.0.1:5432/express_starter_test"
RUN_DATABASE_TESTS=true
ALLOW_REMOTE_TEST_DATABASE=false
