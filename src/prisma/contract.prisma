// use prisma-next
model User {
  id        String                     @id @default(uuid())
  email     String                     @unique
  username  String?
  full_name String?
  password  String
  createdAt TimestamptzString          @default(now())
  updatedAt temporal.updatedAtString()
}
